import 'package:flutter/material.dart';

class AppScreenData {
  final Widget screen;
  final String title;
  final IconData icon;
  final IconData activeIcon;

  AppScreenData(this.screen, this.title, this.icon, this.activeIcon);
}

